import { FiltroComprasPipe } from './filtro-compras.pipe';

describe('FiltroComprasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroComprasPipe();
    expect(pipe).toBeTruthy();
  });
});
