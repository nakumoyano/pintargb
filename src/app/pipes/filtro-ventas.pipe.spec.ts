import { FiltroVentasPipe } from './filtro-ventas.pipe';

describe('FiltroVentasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroVentasPipe();
    expect(pipe).toBeTruthy();
  });
});
